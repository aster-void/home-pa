{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.14.0/.schema/devbox.schema.json",
  "packages": ["bun@latest", "mongodb-7_0@latest"],
  "shell": {
    "init_hook": ["echo 'Hello! It's devbox.'"],
    "scripts": {
      "dev": "bun run dev",
      "build": "bun run build",
      "test": "bun run test"
    }
  }
}
